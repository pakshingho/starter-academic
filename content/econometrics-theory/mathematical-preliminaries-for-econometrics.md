---
title: 1. Mathematical Preliminaries for Econometrics
linktitle: 1. Preliminaries
toc: false
type: docs
date: 2026-04-17
lastmod: 2026-04-17
draft: false
math: true
menu:
  econometrics-theory:
    identifier: chapter-preliminaries
    weight: 2
weight: 2
---

This chapter begins with the matrix and probability tools used repeatedly throughout the rest of the course. For now, Sections 1.1 and 1.2 are filled in, while Section 1.3 remains as an outline placeholder.

## 1.1 Matrix notation and useful results

Following the note, vectors are treated as column vectors by default unless a transpose is written explicitly. A generic $p \times 1$ vector is written as

<div>$$x = \begin{pmatrix} x_1 \\ x_2 \\ \vdots \\ x_p \end{pmatrix}$$</div>

and the inner product of two vectors $x$ and $y$ is

<div>$$\langle x, y \rangle = \sum_{i=1}^p x_i y_i.$$</div>

A $p \times q$ matrix $A$ may be written by columns as

<div>$$A = (a_1, \dots, a_q) = (a_{ij}),$$</div>

where each $a_j$ is a $p \times 1$ column vector. Its transpose is

<div>$$A^\prime = (a_{ji}).$$</div>

If $A$ is $p \times q$ and $B$ is $q \times r$, then the product $AB$ is defined entrywise by

<div>$${(AB)}_{ij} = \sum_{k=1}^q a_{ik} b_{kj}.$$</div>

The key reminder is that matrix multiplication is generally not commutative:

<div>$$AB \neq BA.$$</div>

Two standard transpose identities that will recur later are

<div>$$(A + B)^\prime = A^\prime + B^\prime, \qquad (AB)^\prime = B^\prime A^\prime.$$</div>

If $A$ is square and $|A| \neq 0$, then its inverse exists and satisfies

<div>$$AA^{-1} = A^{-1}A = I_p.$$</div>

For invertible conformable matrices,

<div>$$(AB)^{-1} = B^{-1}A^{-1}.$$</div>

### Rank

The rank $r(A)$ is the maximum number of linearly independent columns, equivalently rows, of $A$.

Useful facts:

- If $|A| \neq 0$ for a square matrix $A$, then $r(A) = p$.
- For conformable matrices, $r(AB) \leq \min(r(A), r(B))$.

### Quadratic forms and positive definiteness

For a square matrix $A$, the quadratic form generated by $A$ is

<div>$$x^\prime A x = \sum_{i=1}^p \sum_{j=1}^p x_i a_{ij} x_j.$$</div>

We say:

- $A$ is positive definite if $x^\prime A x > 0$ for all $x \neq 0$.
- $A$ is positive semidefinite if $x^\prime A x \geq 0$ and equality holds for at least one nonzero $x$.

The ordering $A > B$ means $A - B$ is positive definite. When both inverses exist, $A > B$ implies

<div>$$B^{-1} > A^{-1}.$$</div>

### Trace

The trace of a square matrix is

<div>$$\operatorname{tr}(A) = \sum_{i=1}^p a_{ii}.$$</div>

One especially useful identity is

<div>$$\operatorname{tr}(AB) = \operatorname{tr}(BA).$$</div>

If $r(A) = q \leq p$, then there exists a nonsingular matrix $B$ such that

<div>$$BAB^\prime = \begin{pmatrix} I_q & 0 \\ 0 & 0 \end{pmatrix}.$$</div>

or equivalently

<div>$$A = B^{-1}\begin{pmatrix} I_q & 0 \\ 0 & 0 \end{pmatrix}(B^\prime)^{-1}.$$</div>

### Eigenvalues and eigenvectors

The eigenvalues of a square matrix $A$ are the solutions $\lambda_i$ to

<div>$$|A - \lambda I| = 0.$$</div>

For each eigenvalue $\lambda_i$, there exists a nonzero vector $x_i$ such that

<div>$$Ax_i = \lambda_i x_i.$$</div>

That vector $x_i$ is an eigenvector. If $\Lambda = \operatorname{diag}(\lambda_1, \dots, \lambda_p)$, then $A$ can be written in diagonalized form as

<div>$$A = C \Lambda C^\prime$$</div>

for some nonsingular matrix $C$ whenever such a decomposition is available.

Useful consequences:

- If all eigenvalues are positive, $A$ is positive definite.
- $|A| = \prod_{i=1}^p \lambda_i$.
- $\operatorname{tr}(A) = \sum_{i=1}^p \lambda_i$.
- The zeros of $|A - B \lambda|$ are the eigenvalues of $B^{-1}A$ when $B$ is nonsingular.

### Matrix norms, symmetric matrices, and square roots

For a $p \times q$ matrix $A$, define the norm by

<div>$$\|A\|^2 = \lambda_{\max}(AA^\prime).$$</div>

If $A$ is positive definite and invertible, then

<div>$$\|A^{-1}\| = \lambda_{\min}(A)^{-1}.$$</div>

If $A$ is symmetric, then:

- all eigenvalues are real
- eigenvectors can be chosen to be real
- $\lambda_{\max}(A) = \sup_x \frac{x^\prime A x}{x^\prime x}$
- $\lambda_{\min}(A) = \inf_x \frac{x^\prime A x}{x^\prime x}$
- $r(A)$ equals the number of nonzero eigenvalues

If $A$ is positive definite, it has a square root $B$ such that

<div>$$B^2 = A.$$</div>

Under spectral decomposition $A = X^\prime \Lambda X$, one convenient representation is

<div>$$B = X^\prime \Lambda^{1/2} X.$$</div>

### Idempotent matrices

A matrix $A$ is idempotent if

<div>$$A^2 = A.$$</div>

Two useful facts are:

- $r(A) = \operatorname{tr}(A)$
- the eigenvalues of $A$ are only $0$ or $1$

### Partitioned inverses and two core identities

For a partitioned matrix

<div>$$A = \begin{pmatrix} A_{11} & A_{12} \\ A_{21} & A_{22} \end{pmatrix}.$$</div>

if $|A_{11}| \neq 0$, then the Schur complement of $A_{11}$ is

<div>$$A_{22:1} = A_{22} - A_{21}A_{11}^{-1}A_{12}.$$</div>

and the inverse can be written as

<div>$$A^{-1} = \begin{pmatrix} A_{11}^{-1}\left[I + A_{12}A_{22:1}^{-1}A_{21}A_{11}^{-1}\right] & -A_{11}^{-1}A_{12}A_{22:1}^{-1} \\ -A_{22:1}^{-1}A_{21}A_{11}^{-1} & A_{22:1}^{-1} \end{pmatrix}.$$</div>

Two identities that become especially useful later in simultaneous-equation algebra are

<div>$$\operatorname{vec}(ABC) = (C^\prime \otimes A)\operatorname{vec}(B).$$</div>

and

<div>$$\operatorname{tr}(ABCD) = \operatorname{vec}(C)^\prime (D \otimes B^\prime)\operatorname{vec}(A^\prime).$$</div>

### 1.1.1 Matrix differentiation

For conformable vectors and matrices:

<div>$$\frac{\partial}{\partial x}(x^\prime Ay) = Ay$$</div>

and

<div>$$\frac{\partial}{\partial x}(x^\prime A x) = (A + A^\prime)x.$$</div>

If $A(\theta)$ depends on a parameter vector $\theta$, one standard derivative identity is

<div>$$\frac{\partial}{\partial \theta} \ln |A_\theta| = \operatorname{tr}\left(A_\theta^{-1} \frac{\partial A_\theta}{\partial \theta}\right).$$</div>

For scalar $\theta$,

<div>$$\frac{\partial}{\partial \theta} A_\theta^{-1} = -A_\theta^{-1}\left(\frac{\partial A_\theta}{\partial \theta}\right)A_\theta^{-1}.$$</div>

The note also records the second-derivative formulas

<div>$$\frac{\partial^2}{\partial \theta^2}\ln |A_\theta| = \operatorname{tr}\left(-A_\theta^{-1}\left(\frac{\partial^2 A_\theta}{\partial \theta^2}\right) - A_\theta^{-1}\left(\frac{\partial A_\theta^\prime}{\partial \theta}\right)A_\theta^{-1}\left(\frac{\partial A_\theta}{\partial \theta}\right)\right).$$</div>

and

<div>$$\frac{\partial^2}{\partial \theta^2} A_\theta^{-1} = A_\theta^{-1}\left(\frac{\partial A_\theta}{\partial \theta} A_\theta^{-1} \frac{\partial A_\theta}{\partial \theta} - \frac{\partial^2 A_\theta}{\partial \theta^2}\right)A_\theta^{-1}.$$</div>

### 1.1.2 Inequalities

The note highlights four inequalities that appear repeatedly in probability and asymptotic arguments.

Cauchy-Schwarz:

<div>$$\left(\sum_{i=1}^n x_i y_i\right)^2 \leq \left(\sum_{i=1}^n x_i^2\right)\left(\sum_{i=1}^n y_i^2\right).$$</div>

Power-sum bound:

<div>$$|a+b|^p \leq C\left(|a|^p + |b|^p\right).$$</div>

where $C$ depends only on $p$.

Holder's inequality:

<div>$$\left|\sum_{i=1}^n x_i y_i\right| \leq \left(\sum_{i=1}^n |x_i|^p\right)^{1/p}\left(\sum_{i=1}^n |y_i|^q\right)^{1/q}, \qquad \frac{1}{p} + \frac{1}{q} = 1.$$</div>

More generally,

<div>$$\left|\sum_{i=1}^n x_i y_i z_i\right| \leq \left(\sum_{i=1}^n |x_i|^p\right)^{1/p}\left(\sum_{i=1}^n |y_i|^q\right)^{1/q}\left(\sum_{i=1}^n |z_i|^r\right)^{1/r}, \qquad \frac{1}{p} + \frac{1}{q} + \frac{1}{r} = 1.$$</div>

Minkowski:

<div>$$\left(\sum_{i=1}^n (x_i + y_i)^k\right)^{1/k} \leq \left(\sum_{i=1}^n x_i^k\right)^{1/k} + \left(\sum_{i=1}^n y_i^k\right)^{1/k}, \qquad x_i, y_i \geq 0,\ k \geq 1.$$</div>

### Kronecker product

The Kronecker product of $A$ and $B$ is defined by

<div>$$A \otimes B = (a_{ij}B).$$</div>

Three useful properties are:

- $(A \otimes B)(C \otimes D) = AC \otimes BD$
- $r(A \otimes B) = r(A)r(B)$
- if $A$ is $q \times q$ and $B$ is $p \times p$, then $|A \otimes B| = |A|^p |B|^q$

## 1.2 Elementary properties in probability

When analyzing econometric models, the data come from random experiments. If the same experiment were repeated, the outcome would not generally be identical from one repetition to the next. The key stabilizing idea is that, over many repetitions, frequencies tend to settle down. That long-run stabilization is the foundation on which the note builds later statistical and asymptotic results.

We begin with the basic probability setup.

### Sample space, sample points, and events

The sample space is the set of all possible outcomes of a random experiment. It is denoted by $\Omega$.

A sample point, denoted $\omega$, is a single element of $\Omega$.

Subsets of $\Omega$ are called events.

The next step is to equip $\Omega$ with a probability measure. The measure $\Pr(\cdot)$ must satisfy:

1. $\Pr(\Omega) = 1$.
2. For pairwise disjoint events $A_i$,  
   <div>$$\Pr\left(\bigcup_{i=1}^{\infty} A_i\right) = \sum_{i=1}^{\infty} \Pr(A_i).$$</div>

If $\Omega$ has only finitely many sample points, then we have the simple but important formula

<div>$$\Pr(A) = \frac{\#\{A\}}{\#\{\Omega\}}.$$</div>

### 1.2.1 Some properties on probability

The note lists the following standard properties.

1. If $A \subseteq B$, then $\Pr(A) \leq \Pr(B)$.
2. <div>$$\Pr(A^c) = 1 - \Pr(A).$$</div>
3. If $A \subseteq B$, then <div>$$\Pr(B \setminus A) = \Pr(B) - \Pr(A).$$</div>
4. <div>$$0 \leq \Pr(A) \leq 1.$$</div>
5. <div>$$\Pr\left(\bigcup_{i=1}^{\infty} A_i\right) \leq \sum_{i=1}^{\infty} \Pr(A_i).$$</div>
6. <div>$$\Pr(A \cup B) = \Pr(A) + \Pr(B) - \Pr(A \cap B).$$</div>
7. If $A_1 \subseteq A_2 \subseteq \cdots$, then  
   <div>$$\lim_{n \to \infty} \Pr(A_n) = \Pr\left(\bigcup_{i=1}^{\infty} A_i\right).$$</div>
8. If $A_i$ is a nonincreasing sequence of sets with $\lim_{i \to \infty} A_i = \bigcap_{i=1}^{\infty} A_i$, then  
   <div>$$\lim_{i \to \infty} \Pr(A_i) = \Pr\left(\lim_{i \to \infty} A_i\right).$$</div>
9. Bonferroni's inequality:  
   <div>$$\Pr\left(\bigcap_{i=1}^{k} A_i\right) \geq 1 - \sum_{i=1}^{k} \Pr(A_i^c).$$</div>

#### Conditional probability

If $\Pr(B) > 0$, then the conditional probability of $A$ given $B$ is

<div>$$\Pr(A \mid B) = \frac{\Pr(A \cap B)}{\Pr(B)}.$$</div>

For disjoint events $A_i$,

<div>$$\Pr\left(\bigcup_{i=1}^{k} A_i \,\middle|\, B\right) = \sum_{i=1}^{k} \frac{\Pr(A_i \cap B)}{\Pr(B)}.$$</div>

#### Independence

The events $A_1, A_2, \dots, A_n$ are independent if

<div>$$\Pr(A_{i_1} \cap \cdots \cap A_{i_k}) = \prod_{j=1}^{k} \Pr(A_{i_j})$$</div>

for every subset of indices $\{i_1, \dots, i_k\}$.

If all $\Pr(A_i) > 0$, this is equivalent to saying

<div>$$\Pr(A_j \mid A_{i_1}, \dots, A_{i_k}) = \Pr(A_j)$$</div>

for any $j \notin \{i_1, \dots, i_k\}$.

### Random variables and induced distributions

A random variable $X$ is a function from the sample space $\Omega$ to $\mathbb{R}$ such that for every Borel set $B$,

<div>$$X^{-1}(B) = \{\omega : X(\omega) \in B\}$$</div>

is an event.

The note's intuition is that we often care not about the raw sample points in $\Omega$, but about a measurable quantity induced by them, such as a student's height. Once a random variable $X$ is defined, it induces a probability measure on $\mathbb{R}$:

<div>$$Q(B) = \Pr\bigl(X^{-1}(B)\bigr) = \Pr\bigl(\omega : X(\omega) \in B\bigr).$$</div>

For an interval $[a,b] \subseteq \mathbb{R}$,

<div>$$Q([a,b]) = \Pr\bigl(X^{-1}[a,b]\bigr).$$</div>

When $a = -\infty$, the note defines the distribution function by

<div>$$F(b) = \Pr((-\infty, b]).$$</div>

If $F$ is differentiable, then its derivative

<div>$$f(x) = \frac{d}{dx}F(x)$$</div>

is the density function.

The key properties of $F$ are:

1. <div>$$F(-\infty) = 0, \qquad F(\infty) = 1.$$</div>
2. If $x \leq y$, then <div>$$F(x) \leq F(y).$$</div>
3. If $x_n \to x^+$, then <div>$$F(x_n) \to F(x).$$</div>

### Characteristic functions and expectations

Every random variable $X$ has a characteristic function:

<div>$$\phi(t) = E\!\left(e^{itX}\right), \qquad t \in \mathbb{R}.$$</div>

The note motivates this using

<div>$$\exp(iz) = \cos(z) + i \sin(z).$$</div>

If $g(x)$ is a function of $x$, we say that the expectation $E[g(X)]$ exists if

<div>$$\int |g(x)|\, dF(x) < \infty.$$</div>

In that case,

<div>$$E[g(X)] = \int g(x)\, dF(x).$$</div>

One useful warning from the note is:

<div>$$E(X/Y) \neq E(X)/E(Y)$$</div>

in general.

### Inequalities used later in probability arguments

The note ends the section by collecting a few inequalities that recur later.

Jensen's inequality:

<div>$$f(E[X]) \leq E[f(X)]$$</div>

when $f$ is convex, meaning

<div>$$f(\alpha x + \beta y) \leq \alpha f(x) + \beta f(y).$$</div>

Equality holds when $X$ is constant.

The note also records the moment bound

<div>$$E\{|X - E(X)|^p\} \leq C\,E\bigl(|X|^p + |E(X)|^p\bigr),$$</div>

and because

<div>$$|E(X)| \leq E|X| \leq \bigl(E|X|^p\bigr)^{1/p},$$</div>

we obtain

<div>$$E\{|X - E(X)|^p\} \leq C\,E|X|^p.$$</div>

Finally, the note lists the covariance-style bound in the following two forms:

<div>$$|E(XY)| \leq \left\{\begin{array}{l}
\bigl(E|X|^2\,E|Y|^2\bigr)^{1/2} \\
\frac{1}{2}\bigl(E|X|^2 + E|Y|^2\bigr).
\end{array}\right.$$</div>

## 1.3 Distribution of quadratic forms of normal random variables
